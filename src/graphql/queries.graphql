query user {
  users {
    name
    email
    gender
  }
}

mutation AddUser($name: String!, $email: String!, $gender: String!) {
  addUser(name: $name, email: $email, gender: $gender) {
    _id
    name
    email
    gender
  }
}
